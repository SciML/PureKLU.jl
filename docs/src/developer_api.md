# Developer API

PureKLU's supported user API is the [API reference](@ref api) together with the
standard `LinearAlgebra.Factorization` operations it returns. The abstract type
below is documented to make its implementation boundary explicit; it is not a
downstream extension point.

```@docs
PureKLU.AbstractKLUFactorization
```
